#import "template.typ": aufgabe, meta, loesung

#aufgabe(meta, "2", [
  Gegeben ist die Matrix

  #math(
    A = \begin[pmatrix](
      1 & 2 \\
      3 & 4
    \end)
  )

  Bestimmen Sie die Determinante.

  #loesung[
    #math(
      \det(A) = 1*4 - 2*3 = -2
    )
  ]
])
